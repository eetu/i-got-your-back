// The playground consumes the library's own pattern catalogue (@anarkisti/igyb/registry) —
// no local duplication. Types are re-aliased to the names the gallery components already use.
export {
	overlays,
	type PatternParam as Param,
	patternById,
	type PatternMeta as PatternEntry,
	patterns,
	themeNames
} from '@anarkisti/igyb/registry';
